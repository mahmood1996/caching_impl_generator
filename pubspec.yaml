name: caching_impl_generator
description: A starting point for Dart libraries or applications.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.2.6

dependencies:
  analyzer: '>=5.2.0 <7.0.0'
  build: ^2.0.0
  caching_impl_annotation:
    git: https://github.com/mahmood1996/caching_impl_annotation.git
  source_gen: ^1.5.0

dev_dependencies:
  lints: ^2.1.0
  test: ^1.24.0
  source_gen_test: ^1.0.6
